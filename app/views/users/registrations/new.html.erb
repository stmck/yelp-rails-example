<div class="sign container2">

<h1>Sign up</h1>
<%#= render "devise/shared/links" %>
<p><span class="fs_0">“vegeres”</span> に会員登録をすると、好きなお店をマイページに保存することができます。</p>
<section>
  <ul class="other_sign">
      <li><a class="" href="/users/auth/facebook"><i class="fa fa-facebook"></i>    <span>Facebook</span>アカウントで登録</a></li>
      <li><a class="" href="/users/auth/twitter"><i class="fa fa-twitter"></i>    <span class="b_tw">twitter</span>アカウントで登録</a></li>
  </ul>
      <!-- <div class="regist_block">
      <h2 class="heading_002">E-mailで登録</h2>
           <p>『vegeres』に会員登録すると、好きなお店をマイページに保存することができます。</p> 
             <p><a class="btn_type1" href="sign_up.html">新規会員登録</a></p>
        </div> -->
</section>

<section>
<h2 class="heading_002">メールアドレスで登録</h2>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

   <div><%= f.label :Name %>
    <%= f.text_field :username, autofocus: true %></div>

  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email, autofocus: true %>
  </div>

 <% if f.object.password_required? %>
    <div class="field">
      <%= f.label :password %>
      <% if @validatable %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %>
      <%= f.password_field :password, autocomplete: "off" %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation %>
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>
  <% end %>

  <div class="actions btn_type1">
    <%= f.submit "会員登録" %>
  </div>
<% end %>
</section>
</div>